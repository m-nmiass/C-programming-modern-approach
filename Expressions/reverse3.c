/*************************************************
*   reverse of three digits without arithmetic   *
*************************************************/

#include <stdio.h>

int main ()
{
	int i, j, k ;
	
	printf ("Enter three digits :");
	scanf ("%1d%1d%1d", &i, &j, &k );

	printf("The reversal is : %d%d%d", k, j, i );
}
